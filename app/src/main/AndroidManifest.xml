<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.convictvs.edisone.firstprinciples">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_pb31"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.StudoActivity"
            android:label="@string/app_name"
            android:parentActivityName="com.convictvs.edisone.firstprinciples.activities.MainActivity">
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.convictvs.edisone.firstprinciples.activities.MainActivity" />
        </activity>
    </application>

</manifest>
